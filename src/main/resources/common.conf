akka {
    loglevel = "INFO"
    log-sent-messages = on
    log-received-messages = on
    actor {
        provider = cluster
    }
    remote {
        artery {
            enabled-transports = tcp
            canonical {
                hostname = "127.0.0.1"
            }
        }
    }
}